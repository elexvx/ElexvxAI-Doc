---
title: API 设计规范
description: 对外接口命名、版本与错误处理标准
---

## 路径与命名

- 使用资源导向命名：`/api/v1/documents/{id}`。
- 复数形式表示集合，单数表示实体。

## 错误码约定

- `400` 参数错误
- `401` 未授权
- `403` 权限不足
- `429` 请求频率受限
- `500` 服务内部错误

## 版本策略

采用 URI 版本控制与向后兼容优先策略，废弃接口提前一个迭代公告。
